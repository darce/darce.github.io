---
index: 1
year: 2026
title: 'Semantic Image Search: Local-First AI Photo Search & Exploration'
subtitle: 'Independent product R&D · macOS'
description: 'A native macOS product for semantic photo search and visual exploration that keeps gallery processing on-device and makes incomplete data explicit.'
details: 'I designed and built a native macOS photo-search product that uses on-device models to retrieve and explore a user’s library while keeping gallery images local. Missing or partial data is shown as such instead of being presented as certainty.'
tags:
  - Product Design
  - Interaction Design
  - Human-AI Interaction
  - SwiftUI
  - Core ML
  - Accessibility
---

I designed and built a native macOS photo-search product that uses on-device models to retrieve and explore a user’s library while keeping gallery images local and showing missing or partial data as incomplete.

My role covered product design, interaction design, technical architecture, and implementation. The stage is a working independent R&D product that is not yet user-tested.

**Screen 3 — Semantic results.** This screen shows a semantic query for a red bicycle and a grid of 64 nearest results.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ [ red bicycle                                           ] (Search)       │
├──────────────────────────────────────────────────────────────────────────┤
│ ▸ Explore                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│ red bicycle                                                              │
│ 64 search results · nearest 64                                           │
│  ●  ●  ●  ●  ●  ●                                                        │
│  ●  ●  ●  ●  ●  ●                                                        │
│  ●  ●  ●  ●  ●  ●                                                        │
└──────────────────────────────────────────────────────────────────────────┘
```

Position conveys ranking without exposing raw cosine scores as confidence. A flat ranking instead shows “No close matches…” as its own state.

## At a glance

<table>
  <thead>
    <tr>
      <th scope="col">Dimension</th>
      <th scope="col">Detail</th>
    </tr>
  </thead>
  <tbody>
    <tr><th scope="row">Role</th><td>Product design, interaction design, technical architecture, implementation</td></tr>
    <tr><th scope="row">Stage</th><td>Working independent R&D product</td></tr>
    <tr><th scope="row">Platform</th><td>macOS</td></tr>
    <tr><th scope="row">Core jobs</th><td>Search, inspect, explore</td></tr>
    <tr><th scope="row">AI</th><td>Local semantic retrieval plus segmentation/detection components</td></tr>
    <tr><th scope="row">Constraint</th><td>Useful on 8 GB Apple Silicon</td></tr>
    <tr><th scope="row">Trust boundary</th><td>Gallery processing stays local</td></tr>
    <tr><th scope="row">Accessibility</th><td>Keyboard and VoiceOver behavior</td></tr>
    <tr><th scope="row">Research status</th><td>Expert/adversarial evaluation done; external user testing not yet run.</td></tr>
  </tbody>
</table>

## Problem

A photo library carries meaning that folder names and filenames rarely capture: place, repeated subjects, visual structure, and relationships across years. People need ways to find and inspect that material without surrendering their galleries to a cloud service.

Cloud AI can improve retrieval, but it moves the privacy boundary away from the person and their device. The product therefore had to run locally on modest Apple Silicon hardware. It also had to be honest when the library, index, model, permission, or metadata was incomplete.

## Product principles

1. **User chooses the boundary.** Selected folders define the searchable set; there is no silent whole-disk scan.
2. **No gallery egress.** Model processing happens locally, and gallery images are designed to stay on the machine; the Evaluation section's privacy checks examine that boundary.
3. **Unknown must look unknown.** Partial and missing states are made explicit in context.
4. **Search plus browse.** A semantic query works alongside exploratory views for material the person has not yet named.
5. **Machine output stays inspectable.** Selecting a photo exposes the underlying facts behind the result.

## Core journey

Choose folder → index → semantic query → inspect result → explore patterns

**Screen 1 — Cold start.** Before a folder is chosen, Search says “No folder yet. Use File → Open Folder to enroll photos, then search here,” Layers says there are no layer packs yet, and no zero-result count is shown.

```text
┌─ Semantic Image Search ──────────────────────────────────────────────────┐
│ ┌─ Layers ──────────┐ ┌────────────────────────────────────────────────┐ │
│ │                   │ │ [ Search query                      ] (Search) │ │
│ │  No layer packs   │ │ No folder yet. Use File → Open Folder to        │ │
│ │  yet.             │ │ enroll photos, then search here.               │ │
│ │                   │ ├────────────────────────────────────────────────┤ │
│ │                   │ │                                                │ │
│ │                   │ │                                                │ │
│ └───────────────────┘ └────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
```

The interface keeps “No folder yet” distinct from “no photo matched”; it does not show a zero-result count before any library exists.

**Screen 2 — Indexing.** A determinate meter reports “Embedding 812 of 1,284” above a two-row photo grid.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ [ Search query                                          ] (Search)       │
│ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░  Embedding 812 of 1,284                   │
├──────────────────────────────────────────────────────────────────────────┤
│  ▢ ▢ ▢ ▢ ▢ ▢                                                             │
│  ▢ ▢ ▢ ▢ ▢ ▢                                                             │
└──────────────────────────────────────────────────────────────────────────┘
```

A determinate meter retains the known extent of indexing, while cancellation prevents an interrupted index from being presented as complete.

## Designing for incomplete truth

**No folder yet.** The start state explains that a folder must be chosen before indexing can begin. Treating it as an ordinary empty result would falsely imply that an existing library had already been searched and contained nothing relevant.

**No matches.** A query can complete successfully without finding a suitable photo. Distinguishing this from an indexing failure tells the user that the selected boundary was searched, while still showing what was searched and how to adjust the query or selection.

**Model unavailable.** Semantic search depends on a local model that may be absent or unusable. Saying “no results” would hide the actual cause and could lead someone to conclude that their photos were inadequate when retrieval was only temporarily unavailable.

**Partial library.** Indexing may cover some photos before interruption, or metadata may exist for only part of the collection. The interface scopes what is included and identifies the cause, so a subset is never presented as the whole library.

**Unavailable volume or permission.** A mounted volume may be offline, removed, or denied to the app. Naming that condition preserves the distinction between inaccessible source material and material that was inspected and found irrelevant.

**Experimental capability.** Some machine-derived behavior is deliberately marked experimental. Its presentation keeps uncertainty visible so preliminary output does not acquire the same authority as established product behavior.

These are separate designed states because each has a different cause, action, and evidence; treating unknown as a designed state prevents absence from masquerading as certainty.

**Screen 6 — Partial-library caveat.** A warning says 312 photos were never opened because their files are missing, usually on an unplugged volume, so dates and places describe only the rest of the library.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ▾ Explore                                                                │
│ [ Similarity | Places | Timeline ]                                       │
│ ⚠ 312 photos were never opened: their files are not where the library    │
│   says they are, usually a volume that is not plugged in. Dates and      │
│   places describe the rest of the library, not all of it.                │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │      ▁▂▅█▆▃▁    ▁▃▄▂▁      ▂▇█▅▂▁     ▁▂▃▁                           │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
```

The caveat qualifies all three plots and names why the library is partial. Its visible and spoken wording share the same source so they cannot drift.

**Screen 8 — Missing model and settings recovery.** The model panel says detection still works but search does not when the exported CLIP and MobileSAM folder is unreadable, names `/Volumes/Butter/cache/sis-coreml-export`, and offers Choose and Reset controls; Settings lists `~/Pictures/2021-iceland`, states that content is neither filtered nor certified safe, and shows an off-by-default option that may send 256px crops to local Ollama at 127.0.0.1 while gallery bytes stay on-device.

```text
┌─ Model folder ───────────────────────────────────────────────────────────┐
│ Where the exported CLIP and MobileSAM packages live. The object detector │
│ ships inside the app, so detection works even when this folder is        │
│ missing; search does not.                                                │
│                                                                          │
│ /Volumes/Butter/cache/sis-coreml-export                                  │
│ This folder is not readable — an unmounted disk, or models that were     │
│ never exported there. Search will report the missing package by name.    │
│                                                                          │
│ ( Choose model folder… )   ( Reset to default )                          │
└──────────────────────────────────────────────────────────────────────────┘

┌─ Settings ───────────────────────────────────────────────────────────────┐
│ Library folders                                                          │
│   ~/Pictures/2021-iceland                              (Remove)          │
│   + Open Folder…                                                         │
│ ──────────────────────────────────────────────────────────────────────── │
│ Memory                                                                   │
│ ──────────────────────────────────────────────────────────────────────── │
│ Models                                                                   │
│                                                                          │
│ Content                                                                  │
│   No photo is tagged, hidden, blurred, or ranked lower for what it       │
│   depicts. This app has no explicit-content classifier, so nothing is    │
│   filtered out and nothing is certified safe — search matches every      │
│   indexed photo on the same terms, and a generated layer name may        │
│   describe a photo plainly.                                              │
│                                                                          │
│ Local layer names                                                        │
│   ☐ Ask local Ollama to name unsure layers                               │
│     Off by default. When on, unsure crops may be sent as a 256px JPEG    │
│     to Ollama on this Mac (127.0.0.1). Gallery bytes never leave the     │
│     device.                                                              │
└──────────────────────────────────────────────────────────────────────────┘
```

The model path and its readability are shown separately, and a missing package is named when search cannot run. Settings states that content is not filtered and identifies the optional local socket, which is off by default.

## Data-rich exploration

**Similarity** answers “what else looks like this?” after a photo is selected. **Places** answers “where did this happen?” for images with usable location information. **Timeline** answers “when does this occur?” and helps reveal temporal patterns.

A selection carries across all three views, so inspection remains anchored while the framing changes. Coverage counts surface missing metadata—for example, photographs without location—so absent fields stay visible. Zooming preserves surrounding context and the viewer’s position within it.

**Screen 4 — A photo selected, details open.** IMG_4417.HEIC is selected and can be opened by double-clicking; its open details show 4032 × 3024, Apple iPhone 13 Pro, 26 mm f/1.5, 2021:07:14 18:22:00, GPS 64.1466°, -21.9426°, an offline mini-map, and surrounding result thumbnails.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ┌────────────────┐  IMG_4417.HEIC                                        │
│ │                │  Selected photo · double-click to open                │
│ │   [ preview ]  │  ▾ Photo details                                      │
│ │                │        Size  4032 × 3024                              │
│ │                │      Camera  Apple iPhone 13 Pro                      │
│ └────────────────┘        Lens  26 mm f/1.5                              │
│                          Taken  2021:07:14 18:22:00                      │
│                            GPS  64.1466°, -21.9426°                      │
│                                 ┌──────────────────────────┐             │
│                                 │        ·   ·             │             │
│                                 │      ────●───────        │             │
│                                 │            ·             │             │
│                                 └──────────────────────────┘             │
├──────────────────────────────────────────────────────────────────────────┤
│  ●  ●  ●  ●  ●  ●                                                        │
└──────────────────────────────────────────────────────────────────────────┘
```

The spacing groups each label with its value, and the details section retains its open or collapsed state as selection changes. The mini-map uses an offline graticule rather than fetched tiles.

**Screen 5 — Explore: Similarity / Places / Timeline.** The first badges read Similarity 47, Places 41, and Timeline 47 because only 41 selected photos have coordinates; the Places state then reads 38, 41, and 41, shows 41 of 1,284 photos with 1,198 lacking coordinates and 45 unreadable, and lists a 41-photo map selection outside the search results last in place order rather than by rank.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ▾ Explore                                                                │
│ [ Similarity  47 | Places  41 | Timeline  47 ]                           │
│                              ↑           ↑                               │
│            41 of the 47 selected photos have coordinates.                │
│            The gap between those two numbers IS the finding.             │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ ▾ Explore  [ Similarity  38 | Places  41 | Timeline  41 ]                │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ 90°N ┌──────────────────┬──────────────────┐                         │ │
│ │      │  ∿∿∿   ·  ∿∿┌─────────────┐∿        │                         │ │
│ │   0° ├───∿∿───────∿│  ●   ● ●●  ∿│──∿∿─────┤                         │ │
│ │      │      ∿∿    ∿└─────────────┘∿  · ∿∿  │                         │ │
│ │ 90°S └──────────────────┴──────────────────┘                         │ │
│ │      180°W               0°              180°E                       │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│ Mark area   ○ 1 photo   ◯ 312 photos                                     │
│ Showing 41 of 1,284 photos. 1,198 have no coordinates, 45 could not be   │
│ read.                                                                    │
├──────────────────────────────────────────────────────────────────────────┤
│ ● Map selection · Showing 41 photos in the map selection. 41 of them are │
│   not in your search results, so they are listed last rather than ranked.│
│                                                       ( Clear region )   │
├──────────────────────────────────────────────────────────────────────────┤
│  ▣ ▣ ▣ ▣ ▣ ▣   ← 41 tiles, place order, unranked                         │
└──────────────────────────────────────────────────────────────────────────┘
```

Per-tab counts preserve the current selection’s context when another view is hidden. Missing coordinates, unreadable files, and files never inspected remain separate explanations rather than one total.

**Screen 7 — Timeline: rest, zoom, truncation.** The all-dates view spans 2019–2025, separates 94 undated photos, and identifies 1,190 dated photos—1,104 from exposure time and 86 from file write time; July 2021 zoom uses daily GMT periods and keeps the 94 undated photos off-axis; the truncated view says three photos before 1962 lie off the left and offers a Before 1962 action.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ▾ Explore     [ Similarity | Places | Timeline ]                         │
│ All dates                                                                │
│ Photos by capture year                             Square-root count scale│
│ ┌──────────────────────────────────────────────────┐ ┌─────────────────┐ │
│ │ 1284┤                                            │ │ Off the time    │ │
│ │  722┤        ▅█                                  │ │ axis            │ │
│ │  321┤   ▂▅   ██ ▇▅                               │ │      ⁇          │ │
│ │   80┤ ▁ ██   ██ ██ ▃▁                            │ │      94         │ │
│ │    0┼─┴──┴───┴┴─┴┴─┴──┴──                        │ │ Undated photos  │ │
│ │     2019 2020 2021 2022 2023 2024 2025           │ └─────────────────┘ │
│ └──────────────────────────────────────────────────┘                     │
│ ⚠ 94 photos have no capture date.                                        │
│ ⓘ 1,190 dated: 1,104 from exposure time, 86 from file write time.        │
│ 🕐 Bars are calendar periods: one bar per calendar year, cut in GMT       │
│    (Gregorian).                                                          │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ ▾ Explore     [ Similarity | Places | Timeline ]                         │
│ All dates › 2021 › Jul 2021                                 [ Zoom out ] │
│ Photos by capture day                              Square-root count scale│
│ 1 July 2021 to 31 July 2021                        [ Zoom to selection ] │
│ Bars per   Year ○──────────● Day    Day                                  │
│ ┌──────────────────────────────────────────────────┐ ┌─────────────────┐ │
│ │   ▃▁  ▂▅█▆▃▁ ▁  0  0  ▁▃▄▂▁   ▂▇█▅▂▁    ▁ ▁▂▃▁   │ │ Off the time    │ │
│ │                 ╌  ╌                             │ │ axis            │ │
│ │ 1    5    9   13   17   21   25   29             │ │      ⁇          │ │
│ └──────────────────────────────────────────────────┘ │      94         │ │
│                                                      │ Undated photos  │ │
│                                                      └─────────────────┘ │
│ ⚠ 94 photos have no capture date.                                        │
│ 🕐 Bars are calendar periods: one bar per calendar day, cut in GMT.       │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ Photos by capture year                                                   │
│ ┌──────────────────────────────────────────────────┐                     │
│ │        ▁▂▅█▆▃▁    ▁▃▄▂▁      ▂▇█▅▂▁     ▁▂▃▁     │                     │
│ │ 1962 1966 ... 2019 2020 2021 2022 2023 2024 2025 │                     │
│ └──────────────────────────────────────────────────┘                     │
│ ⚠ 3 photos dated before 1962 are off the left of this axis.              │
│                                       [ Before 1962 ]                    │
└──────────────────────────────────────────────────────────────────────────┘
```

The breadcrumb records the requested window while the range states what the chart draws. Undated photos stay off the time axis, and truncation names what lies outside it and offers a route there.

## Evaluation

### Technical evidence

Performance and runtime behavior were evaluated against an 8 GB Apple Silicon constraint. Privacy checks found no network egress of gallery data in the tested flows. Model and licence constraints were reviewed, and automated tests covered core behavior.

### Interaction and expert evidence

A whole-product UX map framed expert review, with targeted review questions attached to each screen to isolate specific interaction risks. Accessibility review addressed keyboard operation and VoiceOver announcements, while adversarial review probed claims and failure states. Automated checks that changed state and confirmed the view updated correctly caught places where the display did not follow a state change.

### External-user evidence

Not yet run. This is the next open evidence gap: a 3–5 person task-based study on search, inspect, and explore.

## Three times evaluation changed the product

- Explore tab switching destroyed the established viewpoint → orientation was lost on every tab switch → persistent viewpoint state moved to the container → verified by a regression check across tab changes.
- Partial-library plots looked like complete-library truth → a subset could read as the whole library → scoped, cause-specific caveat plus a VoiceOver announcement → verified by the accessibility review.
- Timeline selection could outlive the axis it referred to → a selection pointed at nothing → positional state cleared when the axis itself changes → verified by a mutation check.

## Outcome and limits

The outcome is a working end-to-end native product with coherent search, inspection, and exploration interactions; local inference; keyboard and VoiceOver behavior; explicit failure states; and a recorded evaluation trail. It does not establish external demand or real-world effectiveness with outside participants.

External users remain untested, and performance beyond the current test corpus remains unmeasured at larger real-library scale. The next experiment should learn whether people can form useful queries, understand result provenance and caveats, retain orientation across exploration views, and recover confidently from incomplete-truth states during task-based use.

> **Screenshots: pending.** The screens above are ASCII, drawn from the shipped code at `fec9fa6` and pinned to shipped strings by a test in the product repository (every quoted string must occur verbatim in the named Swift file). Screenshots are being captured; this note remains visible so the case does not imply media that does not exist.
